"""Placeholder worker entrypoint."""

from __future__ import annotations


def run_worker(run_id: int) -> None:
    """Placeholder worker logic."""
    print(f"Running crawl worker for run {run_id}")
